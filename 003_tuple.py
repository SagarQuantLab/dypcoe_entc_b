#TUPLE
# (), index, ordered, duplicates allowed, Immutable (non-mutable)

my_tuple = tuple([1, 4, 5, 8, 3, 3])
my_tuple = (1, 4, 5, 8, 3, 3)

# access first element
print(my_tuple[0])

# access last element
print(my_tuple[-1])

# duplicates allowed or not
print(my_tuple)

# assign 1000 to first element
# my_tuple[0] = 1000